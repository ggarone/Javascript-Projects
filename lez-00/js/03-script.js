var colors = ['bianco', 'rosso', 'blu'];

var colors_ = new Array('bianco', 'rosso', 'blu');

// console.log(colors);

colors.push('nero');

for (let i = 0; i < colors.length; i++) {
    console.log(colors[i]);
}

colors.sort();

colors.reverse();


